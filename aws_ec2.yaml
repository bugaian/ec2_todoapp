plugin: aws_ec2
regions:
  - eu-central-1
filters:
  tag:Env: ansible
    # All instances with their `Environment` tag set to `dev`
    #   tag:Environment: dev
    #     # All dev and QA hosts
    #       tag:Environment:
